spring:
  profiles:
    active: ${profile:test}
  application:
    name: World Cup Simulator Test Application
  datasource:
    url: "jdbc:postgresql://127.0.0.1:5432/simulator_test?reWriteBatchedInserts=true"
    username: postgres
    password: s3cr3t
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        # FIX FOR: java.sql.SQLFeatureNotSupportedException: Method org.postgresql.jdbc.PgConnection.createClob() is not yet implemented.
        # Disable feature detection by this undocumented parameter. Check the org.hibernate.engine.jdbc.internal.JdbcServiceImpl.configure method for more details.
        temp.use_jdbc_metadata_defaults: false
        # Because detection is disabled you have to set correct dialect by hand.
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
        jdbc.batch_size: 20
